namespace EntityToDto.Conventions
{
    /// <summary>
    /// Map between types by their property names.
    /// Thus so, because C# does not allow the same property name to exist in a type.
    /// </summary>
    public class PropertyNameMappingConvention : IMapConvention
    {
    }
}
